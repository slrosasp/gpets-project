version: '3.8'

services:
  gpets:
    build: ./gpets-api
    container_name: gpets-app
    ports:
      - "8080:8080"
    environment:
      - FIREBASE_DATABASE_URL=${FIREBASE_DB_URL}
    volumes:
      - ./gpets-api/src/main/resources/firebase-service-account.json:/app/firebase-service-account.json:ro